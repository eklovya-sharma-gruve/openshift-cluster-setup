apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: loki-operator-operatorgroup
  namespace: openshift-operators-redhat
spec:
  targetNamespaces:
    - openshift-operators-redhat